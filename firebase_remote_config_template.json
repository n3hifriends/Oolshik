{
  "conditions": [
    {
      "name": "android_only",
      "expression": "device.os == 'android'",
      "tagColor": "GREEN"
    },
    {
      "name": "ios_only",
      "expression": "device.os == 'ios'",
      "tagColor": "BLUE"
    }
  ],
  "version": {
    "versionNumber": "9",
    "updateTime": "2026-06-21T11:49:20.605502Z",
    "updateUser": {
      "email": "n3.hifriends@gmail.com"
    },
    "updateOrigin": "CONSOLE",
    "updateType": "FORCED_UPDATE"
  },
  "parameterGroups": {
    "Operational Controls": {
      "description": "App-wide gates and performance tuning.",
      "parameters": {
        "maintenance_mode_enabled": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Show full-screen maintenance gate to all users. Set maintenance_message first before enabling.",
          "valueType": "BOOLEAN"
        },
        "maintenance_message": {
          "defaultValue": {
            "value": ""
          },
          "description": "Message shown on maintenance screen. Falls back to hardcoded default if empty.",
          "valueType": "STRING"
        },
        "min_supported_app_version_android": {
          "defaultValue": {
            "value": "0.0.0"
          },
          "conditionalValues": {
            "android_only": {
              "value": "0.0.0"
            }
          },
          "description": "Minimum supported Android version (semver). Raise above current build to trigger force update. Requires force_update_enabled=true.",
          "valueType": "STRING"
        },
        "min_supported_app_version_ios": {
          "defaultValue": {
            "value": "0.0.0"
          },
          "conditionalValues": {
            "ios_only": {
              "value": "0.0.0"
            }
          },
          "description": "Minimum supported iOS version (semver). Raise above current build to trigger force update. Requires force_update_enabled=true.",
          "valueType": "STRING"
        },
        "force_update_enabled": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Additional gate for force update — must be true AND version below minimum. Prevents a single mis-click from blocking all users.",
          "valueType": "BOOLEAN"
        },
        "audio_upload_use_presigned": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Use S3 presigned PUT for audio upload (true) or server-buffered chunked upload (false).",
          "valueType": "BOOLEAN"
        },
        "help_request_poll_interval_ms": {
          "defaultValue": {
            "value": "10000"
          },
          "description": "Transcription polling interval in milliseconds. Clamped app-side to [5000, 60000].",
          "valueType": "NUMBER"
        },
        "remote_config_fetch_interval_seconds": {
          "defaultValue": {
            "value": "3600"
          },
          "description": "How often the app re-fetches Remote Config (seconds). Clamped app-side to [300, 86400]. Does not apply in dev builds.",
          "valueType": "NUMBER"
        },
        "nearby_help_poll_interval_ms": {
          "defaultValue": {
            "value": "45000"
          },
          "description": "How often the home feed polls for nearby requests while the screen is focused and the app is in the foreground (milliseconds). Clamped app-side to [15000, 300000].",
          "valueType": "NUMBER"
        },
        "nearby_help_min_location_delta_meters": {
          "defaultValue": {
            "value": "100"
          },
          "description": "Minimum distance the device must move before a location change triggers a new nearby fetch (metres). Prevents GPS jitter from causing unnecessary requests.",
          "valueType": "NUMBER"
        }
      }
    },
    "Auth Flags": {
      "description": "Control login method availability. UX only — backend validates all tokens.",
      "parameters": {
        "auth_phone_otp_enabled": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Show OTP phone login option. UX only — backend validates tokens regardless.",
          "valueType": "BOOLEAN"
        },
        "auth_google_enabled": {
          "defaultValue": {
            "value": "true"
          },
          "description": "Show Google Sign-In option. UX only — backend validates tokens regardless.",
          "valueType": "BOOLEAN"
        },
        "auth_google_require_phone": {
          "defaultValue": {
            "value": "true"
          },
          "description": "Require phone number collection after Google sign-in.",
          "valueType": "BOOLEAN"
        }
      }
    },
    "Debug Flags": {
      "description": "Dev and staging only. Both require __DEV__ app-side guard — never enable in production.",
      "parameters": {
        "mock_nearby_enabled": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Use mock nearby tasks instead of real API. App-side guard also requires __DEV__. Never enable in production Console.",
          "valueType": "BOOLEAN"
        },
        "mock_upload_create_enabled": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Use mock upload/create API client. App-side guard also requires __DEV__. Never enable in production Console.",
          "valueType": "BOOLEAN"
        }
      }
    },
    "Privacy and Observability": {
      "description": "Defaults match production-safe behavior.",
      "parameters": {
        "analytics_debug_logging_enabled": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Enable Analytics collection in dev/debug builds for DebugView output. Never enable in production.",
          "valueType": "BOOLEAN"
        },
        "crashlytics_user_context_enabled": {
          "defaultValue": {
            "value": "true"
          },
          "description": "Attach user ID and attributes to crash reports. Set false for privacy-sensitive regions or user opt-out.",
          "valueType": "BOOLEAN"
        }
      }
    },
    "UI and Content": {
      "description": "Dynamic strings and banners without a release.",
      "parameters": {
        "home_banner_enabled": {
          "defaultValue": {
            "value": "false"
          },
          "description": "Show a dismissable announcement banner on the home screen.",
          "valueType": "BOOLEAN"
        },
        "home_banner_title": {
          "defaultValue": {
            "value": ""
          },
          "description": "Title text for the home screen banner. Only shown when home_banner_enabled is true.",
          "valueType": "STRING"
        },
        "home_banner_message": {
          "defaultValue": {
            "value": ""
          },
          "description": "Body text for the home screen banner. Only shown when home_banner_enabled is true.",
          "valueType": "STRING"
        },
        "support_contact_whatsapp": {
          "defaultValue": {
            "value": ""
          },
          "description": "WhatsApp number for support contact (e.g. +919876543210). Empty hides the WhatsApp link.",
          "valueType": "STRING"
        },
        "support_contact_email": {
          "defaultValue": {
            "value": ""
          },
          "description": "Email address for support contact. Empty hides the email link.",
          "valueType": "STRING"
        },
        "home_banner_type": {
          "defaultValue": {
            "value": "info"
          },
          "description": "Visual intent of the banner. Accepted values: info, warning, success, urgent. Controls accent colour and background.",
          "valueType": "STRING"
        },
        "home_banner_id": {
          "defaultValue": {
            "value": ""
          },
          "description": "Campaign key for the banner. Change this value to reset per-device dismiss state without toggling home_banner_enabled.",
          "valueType": "STRING"
        },
        "home_banner_auto_dismiss_seconds": {
          "defaultValue": {
            "value": "0"
          },
          "description": "Seconds before the banner auto-dismisses. 0 disables auto-dismiss and requires the user to tap the X.",
          "valueType": "NUMBER"
        },
        "feature_help_requests_disabled_message": {
          "defaultValue": {
            "value": ""
          },
          "description": "Message shown when help requests are temporarily disabled. Empty falls back to app-local copy.",
          "valueType": "STRING"
        }
      }
    },
    "Feature Flags": {
      "description": "Control feature availability without an app release.",
      "parameters": {
        "feature_help_requests_enabled": {
          "defaultValue": {
            "value": "true"
          },
          "description": "Show/hide the help requests flow for all users. UX gate only — backend still enforces access.",
          "valueType": "BOOLEAN"
        },
        "feature_audio_upload_enabled": {
          "defaultValue": {
            "value": "true"
          },
          "description": "Enable/disable audio upload. Set false to suppress the feature globally without a release.",
          "valueType": "BOOLEAN"
        },
        "feature_location_capture_enabled": {
          "defaultValue": {
            "value": "true"
          },
          "description": "Enable/disable foreground location capture and permission request.",
          "valueType": "BOOLEAN"
        },
        "feature_push_registration_enabled": {
          "defaultValue": {
            "value": "true"
          },
          "description": "Enable/disable FCM token registration. Set false for emergency suppression or backend instability.",
          "valueType": "BOOLEAN"
        }
      }
    }
  }
}