export async function transcribeAudio(_filePath: string): Promise<string> {
  await new Promise((resolve) => setTimeout(resolve, 0))
  // return "बाजारातून भाजी आणि दूध आण. Also remind me to switch on the water pump."
  return "..."
}
